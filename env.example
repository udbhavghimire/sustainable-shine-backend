# ===========================================
# EXAMPLE ENVIRONMENT VARIABLES
# ===========================================
# Copy this file and rename to .env for local development
# For production, set these in your hosting platform (Railway, Render, etc.)

# Django Settings
# Generate a new SECRET_KEY for production using:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-this-to-something-secure

# Set to False in production
DEBUG=False

# Add your domain(s) separated by commas (no spaces)
ALLOWED_HOSTS=api.sustainableshine.com.au,your-railway-domain.up.railway.app

# Database
# Railway/Render will automatically provide this
# For local: DATABASE_URL=sqlite:///db.sqlite3
DATABASE_URL=postgresql://user:password@host:port/database

# CORS Origins (your frontend URLs) - separated by commas (no spaces)
CORS_ALLOWED_ORIGINS=https://sustainableshine.com.au,https://www.sustainableshine.com.au

# Optional: Email Settings (for sending notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Optional: AWS S3 (for media file storage in production)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=ap-southeast-2

